const userBbox = () => {
    let boxS = document.getElementById("user-box")  //get the infromation from an HTML element
    user-box.addEventListener("submit", (event) => {
            event.preventDefault(); //prevent the userBbox from submiting 
        } 

};

const name = document.getElementById("nameInput");

console.log("submitted name: ", name);


const userBbox = () => {
    let boxS = document.getElementById("userr_body"); //gets the information from an HTML element
    user-box.addEventListener("submit", (event) => {
            event.preventDefault(); //prevent the userBbox from submiting 
        } 

};

const nameInput = document.getElementById("anyNameInput");

console.log("submitted name: ", nameInput);

//Next 